import { CAREER_INFO } from '../user';

export const RECENT_ROLE = CAREER_INFO.currentRole;
export const CAREER_GOAL = CAREER_INFO.goal;

export const CTA_BUTTON = {
  title: 'reach out now',
  href: '/about',
};